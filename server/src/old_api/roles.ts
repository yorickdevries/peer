/**
 * Enum that specifies the roles.
 * Please note that this file is not used in the database sql dump.
 * Changing the names here may result in a total failure.
 */
export enum Roles {
    teacher = "teacher",
    TA = "TA",
    student = "student"
}